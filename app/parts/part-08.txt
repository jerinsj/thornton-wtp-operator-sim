d(1)+' mg/L';$('ammoniaRead').textContent=ammonia.toFixed(2)+' mg/L';$('phRead').textContent=ph.toFixed(2);$('speedRead').textContent=controls.speed.value+'×';

    $('rawTurb').textContent=state.rawTurb.toFixed(1)+' NTU';$('settledTurb').textContent=state.settled.toFixed(2)+' NTU';$('filterTurb').textContent=state.filtered.toFixed(3)+' NTU';$('finishCl').textContent=state.finishCl.toFixed(2)+' mg/L';
    $('rawTurbStage').textContent=state.rawTurb.toFixed(1)+' NTU';$('settledStage').textContent=state.settled.toFixed(2)+' NTU';$('ozoneStage').textContent=(ozone*(state.event==='ozfault'?.55:1)).toFixed(1)+' mg/L';$('filterStage').textContent=state.filtered.toFixed(2)+' NTU';$('clStage').textContent=state.finishCl.toFixed(1)+' mg/L';
    const optimal=12.5+state.rawTurb*.52+state.rawTOC*.55-state.jarBonus, err=Math.abs(coag-optimal);
    $('flocStage').textContent=err<2?'GOOD':err<4?'FAIR':'POOR';
    $('finishStage').textContent=(state.filtered<.15&&state.finishCl>1.2&&state.finishCl<3.6)?'STABLE':'CHECK';

    $('rawTurbBar').style.width=Math.min(100,state.rawTurb/15*100)+'%';$('settledBar').style.width=Math.min(100,state.settled/2*100)+'%';$('filterBar').style.width=Math.min(100,state.filtered/.3*100)+'%';$('clBar').style.width=Math.min(100,state.finishCl/4.5*100)+'%';
    drawTrend(); renderFilters(); renderDistribution();
  }
  function drawTrend(){
    const svg=$('trendSvg'); const T=state.historyT,C=state.historyC;if(T.length<2){svg.innerHTML='';return}
    const n=T.length-1;
    const ptsT=T.map((v,i)=>`${i/n*800},${105-Math.min(.3,v)/.3*90}`).join(' ');
    const ptsC=C.map((v,i)=>`${i/n*800},${105-Math.min(4.5,v)/4.5*90}`).join(' ');
    svg.innerHTML=`<polyline points="${ptsT}" fill="none" stroke="#71e1ff" stroke-width="2"/><polyline points="${ptsC}" fill="none" stroke="#ffcf5a" stroke-width="2"/><line x1="0" y1="70" x2="800" y2="70" stroke="#17364c" stroke-width="1"/><line x1="0" y1="35" x2="800" y2="35" stroke="#17364c" stroke-width="1"/>`;
  }
  function showModal(title,body,buttons='<button class="btn" onclick="document.getElementById(\'modal\').classList.remove(\'show\')">Close</button>'){
    $('modalBox').innerHTML=`<h2>${title}</h2>${body}<div class="row">${buttons}</div>`;$('modal').classList.add('show');
  }
  $('sampleBtn').onclick=()=>{
    state.sampleCount++; state.response=Math.min(100,state.response+2);
    const freeAmmonia=Math.max(.01,(+controls.ammonia.value)-(+controls.chlorine.value/5.2)*.15);
    showModal('Shift Lab Check',`<div class="labgrid">
      <div class="labres"><span>Settled turbidity</span><b>${state.settled.toFixed(2)} NTU</b></div>
      <div class="labres"><span>Filtered turbidity</span><b>${state.filtered.toFixed(3)} NTU</b></div>
      <div class="labres"><span>Finished disinfectant</span><b>${state.finishCl.toFixed(2)} mg/L</b></div>
      <div class="labres"><span>Finished pH</span><b>${state.finishPH.toFixed(2)}</b></div>
      <div class="labres"><span>Sim free ammonia</span><b>${freeAmmonia.toFixed(2)} mg/L</b></div>
      <div class="labres"><span>Raw TOC index</span><b>${state.rawTOC.toFixed(1)}</b></div>
    </div><p>These values are generated by the game model, not actual Thornton operational targets.</p>`);
    log('Completed shift laboratory check.');
    if(state.event==='analyzer'||state.event==='clfeed') state.response=Math.min(100,state.response+1);
  };
  $('inspectBtn').onclick=()=>{
    const worst=[...state.filters].sort((a,b)=>(b.head+b.run/70)-(a.head+a.run/70))[0];
    state.response=Math.min(100,state.response+2);
    showModal('Field Inspection',`<p>Walkdown complete. The highest-attention unit is <b>Filter ${worst.id}</b>: ${worst.run.toFixed(0)} run hours, ${worst.head.toFixed(1)} ft simulated headloss, ${worst.turb.toFixed(3)} NTU effluent.</p><p>Ozone room status: <b>${state.event==='ozfault'?'DERATED':'normal'}</b>. Chemical feed visual check: <b>${state.event==='clfeed'?'chlorine output appears low':'no obvious issue'}</b>.</p>`);
    log('Completed field inspection.');
    if(['ozfault','coagfeed','filterbreak','sludge','power','sourcepump'].includes(state.event)) state.response=Math.min(100,state.response+1);
  };
  $('jarBtn').onclick=()=>{
    const opt=12.5+state.rawTurb*.52+state.rawTOC*.55; state.jarBonus=.8; state.response=Math.min(100,state.response+3);
    showModal('Jar Test Result',`<p>Best simulated clarification occurred near <b>${opt.toFixed(1)} mg/L</b> coagulant under current source conditions.</p><p>Use this as a game hint only; real plants require approved procedures, actual chemistry and site-specific testing.</p>`);
    log('Completed jar test; coagulant response curve updated.');
  };
  $('distBtn').onclick=()=>{
    const minZone=[...state.zones].sort((a,b)=>a.pressure-b.pressure)[0];
    const lowTank=[...state.tanks].sort((a,b)=>a.level-b.level)[0];
    const tripped=state.pumps.filter(p=>p.trip).map(p=>p.label).join(', ')||'none';
    const total=state.twtpNet+state.otherSupply;
    const balance=total-state.cityDemand;
    const storagePct=avgStorage();
    const suggested=clamp(state.cityDemand-state.otherSupply+(0.60-storagePct)*8,5,20);
    state.response=Math.min(100,state.response+2);
    showModal('Distribution System Check',`<div class="labgrid">
      <div class="labres"><span>System demand</span><b>${state.cityDemand.toFixed(1)} MGD</b></div>
      <div class="labres"><span>Total modeled supply</span><b>${total.toFixed(1)} MGD</b></div>
      <div class="labres"><span>Average storage</span><b>${Math.round(storagePct*100)}%</b></div>
      <div class="labres"><span>Lowest zone pressure</span><b>Z${minZone.id} · ${minZone.pressure.toFixed(0)} psi</b></div>
      <div class="labres"><span>Lowest tank</span><b>${lowTank.id} · ${lowTank.level.toFixed(0)}%</b></div>
      <div class="labres"><span>PRV status</span><b>${state.prv.abnormal?'ABNORMAL':'NORMAL'}</b></div>
    </div><p>Distribution pump trips: <b>${tripped}</b>. Current supply/storage balance is <b>${balance>=0?'+':''}${balance.toFixed(1)} MGD</b>.</p><p>Game dispatch hint: TWTP near <b>${suggested.toFixed(1)} MGD</b> would move the modeled system toward balanced storage under current conditions. Protect treatment performance while watching individual tank and zone trends.</p>`);
    log('Reviewed five pressure-zone families, nine tank levels, seven distribution pumps and PRV status.');
  };
  $('dispatchBtn').onclick=()=>{
    if(state.event==='mainbreak'){
      state.eventTimer=Math.min(state.eventTimer,25);state.response=Math.min(100,state.response+6);
      log(`Field distribution crew dispatched to investigate the Zone ${state.zones[state.eventZone].id} pressure/demand anomaly.`);
      showModal('Field Response Dispatched',`<p>Distribution maintenance has been sent to investigate the simulated pressure loss / increased flow in Zone ${state.zones[state.eventZone].id}.</p><p>Continue watching affected tank levels, zone pressure and total demand while the field crew isolates the problem.</p>`);
    }else if(state.event==='prvfault'){
      state.eventTimer=Math.min(state.eventTimer,20);state.response=Math.min(100,state.response+5);
      log(`Field crew dispatched to verify pressure-control equipment in Zone ${state.zones[state.eventZone].id}.`);
      showModal('PRV / Pressure Response',`<p>A field crew is checking the simulated pressure-control abnormality affecting Zone ${state.zones[state.eventZone].id}.</p><p>Do not chase the pressure indication with unnecessary plant-flow changes while the cause is being verified.</p>`);
    }else if(state.event==='fireflow'){
      state.response=Math.min(100,state.response+3);
      log(`Confirmed high-demand / fire-flow condition in Zone ${state.zones[state.eventZone].id}; distribution response notified.`);
      showModal('Demand Event Confirmed',`<p>The high flow in Zone ${state.zones[state.eventZone].id} has been confirmed as a temporary fire-flow condition. Continue protecting zone pressure and storage.</p>`);
    }else{
      showModal('No Field Dispatch Needed',`<p>No active distribution condition currently requires a simulated field dispatch. Continue monitoring tank levels, pressures, pump status and demand.</p>`);
    }
  };
  $('bwFaultRespondBtn').onclick=()=>{
    const s=state.bwSeq;
    if(!s.fault)return;
    showModal('Backwash Actuator Fault',`<p><b>${s.fault.message}</b></p><p>The automatic sequence is in HOLD. Choose the safest high-level operator response.</p>
      <div class="incident-options">
        <button class="btn incident-choice" id="bwFaultCorrect">Keep sequence in HOLD, compare command vs position feedback, inspect/coordinate maintenance, then retry only after the valve is verified.</button>
        <button class="btn incident-choice" id="bwFaultWrong1">Bypass the position permissive and force the next sequence step.</button>
        <button class="btn incident-choice" id="bwFaultWrong2">Start another filter backwash while this one is held.</button>
      </div>`);
    $('bwFaultCorrect').onclick=()=>{
      state.response=Math.min(100,state.response+6);
      log(`Backwash fault response: verified ${s.fault.name} position/actuation and coordinated corrective action before retry.`);
      s.fault=null;s.paused=false;
      clearAlarm('bwfault',true);
      $('modal').classList.remove('show');
      renderFilters();
    };
    const bad=()=>{
      state.response=Math.max(0,state.response-6);
      const r=document.createElement('div');r.className='incident-result';
      r.innerHTML='<b>Not the preferred response.</b> Do not bypass position feedback or start another wash while the sequence is held. Keep the sequence safe, verify the failed actuator, and coordinate repair/inspection.';
      const box=$('modalBox');const old=box.querySelector('.incident-result');if(old)old.remove();box.insertBefore(r,box.querySelector('.row'));
    };
    $('bwFaultWrong1').onclick=bad;$('bwFaultWrong2').onclick=bad;
  };

  $('pfasSampleBtn').onclick=()=>{
    if(state.pfas.samplePending){
      showModal('PFAS Sample Already Pending',`<p>A PFAS process sample is already in the simulated laboratory queue.</p><p>PFAS is not treated as a real-time analyzer value in this game.</p>`);
      return;
    }
    state.pfas.samplePending=true;
    state.pfas.sampleTimer=180;
    state.response=Math.min(100,state.response+1);
    log('Collected PFAS GAC process sample and submitted it to the simulated laboratory.');
    showModal('PFAS Process Sample Collected',`<p>Pre- and post-GAC samples were submitted to the simulated water-quality laboratory.</p><p>For gameplay, results return after several simulated hours. Real PFAS analysis is a specialized laboratory process, not a continuous plant analyzer.</p>`);
    renderPFAS();
    renderRoutineTasks();
  };
  $('pfasInspectBtn').onclick=()=>{
    state.response=Math.min(100,state.response+1);
    showModal('PFAS GAC Facility Inspection',`<p><b>Mode:</b> ${state.pfas.mode}</p><p><b>Flow:</b> ${state.pfas.flow.toFixed(1)} MGD<br><b>Modeled headloss:</b> ${state.pfas.headloss.toFixed(1)} ft<br><b>Media service remaining:</b> ${Math.round(state.pfas.mediaRemaining*100)}%</p><p>${state.pfas.headloss>4.5?'Hydraulic resistance is elevated; coordinate further inspection/maintenance under the approved procedure.':'No obvious hydraulic abnormality is indicated.'}</p>`);
    log('Completed PFAS GAC facility operator inspection.');
  };
  $('pfasMediaBtn').onclick=()=>{
    const status=state.pfas.mediaRemaining>.45?'NORMAL':state.pfas.mediaRemaining>.25?'WATCH':'PLAN MEDIA CHANGEOUT';
    showModal('PFAS GAC Media Service Review',`<p><b>Modeled service remaining:</b> ${Math.round(state.pfas.mediaRemaining*100)}%</p><p><b>Bed volumes treated:</b> ${Math.round(state.pfas.bedVolumes).toLocaleString()}</p><p><b>Breakthrough status:</b> ${state.pfas.breakthrough}</p><p><b>Planning status:</b> ${status}</p><p>Media replacement/reactivation is treated as planned maintenance, not a routine operator dose adjustment.</p>`);
    log('Reviewed PFAS GAC media service indicators and laboratory trend.');
  };

  $('shutdownBtn').onclick=()=>{
    controls.flow.value=8; state.response=Math.max(0,state.response-1);log('Operator reduced plant flow to 8.0 MGD.');
    render();
  };
  function endShift(){
    state.ended=true;
    const unfinished=ROUTINE_TASKS.filter(t=>!t.completed);
    if(unfinished.length)state.routineScore=Math.max(0,state.routineScore-unfinished.length*2);
    const completedTasks=ROUTINE_TASKS.filter(t=>t.completed).length;
    state.score=Math.max(0,Math.round(state.quality*.34+state.distribution*.22+state.efficiency*.14+state.response*.18+state.routineScore*.12 + state.sampleCount*1.2));
    const grade=state.score>=90?'A':state.score>=80?'B':state.score>=70?'C':'Needs improvement';
    let summary=`<p>Your simulated 12-hour shift is complete.</p><div class="labgrid">
      <div class="labres"><span>Final score</span><b>${state.score}</b></div>
      <div class="labres"><span>Grade</span><b>${grade}</b></div>
      <div class="labres"><span>Water quality</span><b>${Math.round(state.quality)}</b></div>
      <div class="labres"><span>Efficiency</span><b>${Math.round(state.efficiency)}</b></div>
      <div class="labres"><span>Alarm response</span><b>${Math.round(state.response)}</b></div>
      <div class="labres"><span>Distribution</span><b>${Math.round(state.distribution)}</b></div>
      <div class="labres"><span>Routine operations</span><b>${Math.round(state.routineScore)}</b></div>
      <div class="labres"><span>Routine tasks</span><b>${completedTasks}/${ROUTINE_TASKS.length}</b></div>
      <div class="labres"><span>Lab checks</span><b>${state.sampleCount}</b></div></div>
      <p>${unfinished.length?`<b>${unfinished.length} routine task(s) were left incomplete.</b> A strong shift includes both process control and ordinary operating work.`:'All scheduled routine tasks were completed.'}</p>`;
    showModal('SHIFT COMPLETE',summary,`<button class="btn good" oncl